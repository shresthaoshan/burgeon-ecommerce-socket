<head>
    <style>
        * {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        main {
            margin: 50px 0;
        }
        table {
            border-collapse: collapse;
            table-layout: auto;
            width: 100%;
            max-width: 600px;
        }
        th, td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid rgba(90, 90, 90, 0.25);
            border-right: 1px solid rgba(90, 90, 90, 0.25);
        }
        th:nth-child(1), td:nth-child(1) {
            border-left: 1px solid rgba(90, 90, 90, 0.25);
            text-align: left;
        }
        th {
            background: rgba(90, 90, 90, 0.25);
        }
        tr:last-child td {
            background: rgba(90, 90, 90, 0.85);
            color: white;
            font-weight: 500;
            padding: 5px 10px;
        }
        table.issuedTo td {
            background: rgba(140, 195, 120, 0);
            border-right: 0px solid black;
            border-left: 0px solid black;
            color: black;
            text-align: left;
            font-weight: 400;
        }
        table.issuedTo td:last-child {
            border-left: 1px solid rgba(90, 90, 90, 0.25);
        }
    </style>
</head>
<body>
    <header>
        <h2>Checkout Invoice</h2>

        <p><i>Issued to:</i></p>
        <table class="issuedTo">
            <tr>
                <td>Name: {{userDetails.name}}</td>
                <td>Email: {{userDetails.email}}</td>
            </tr>
        </table>
    </header>

    <main>
        <p>Checkout details:</p>

        <table>
            <tr>
                <th>Item</th>
                <th>Rate</th>
                <th>Quantity</th>
            </tr>
            {% for item in items %}
                <tr>
                    <td>{{ item.name }}</td>
                    <td>Rs.{{ item.price }}/-</td>
                    <td>{{ item.quantity }}</td>
                </tr>
            {% endfor %}
            <tr>
                <td>Total Due:</td>
                <td></td>
                <td><b>Rs.{{ totalAmount }}/-</b></td>
            </tr>
        </table>
    </main>

    <footer>
        <p>Thank you for shopping with us. We hope to serve you in the future again.</p>
    </footer>
</body>